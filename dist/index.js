'use strict';

var createErrorDefinitions = require('./error-definitions');
var createError = require('./create-error');

module.exports = {
    createErrorDefinitions: createErrorDefinitions,
    createError: createError
};